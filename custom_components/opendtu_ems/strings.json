{
  "config": {
    "step": {
      "user": {
        "title": "OpenDTU Zero-Export EMS",
        "description": "The EMS itself is a single Home Assistant **package** file. Setup installs it as `{package}` and the ready-made dashboard view as `{dashboard}`.\n\nAn existing file is **never replaced here** — the EMS is meant to be edited (section 1 holds your entity ids), so setup only creates what is missing. When a newer package is bundled you get a notification and apply it yourself with the action `opendtu_ems.install_bundle`, which keeps the current file as a `.bak` copy.\n\nAfterwards **restart Home Assistant** so the entities (sensors, helpers, automations) are created.",
        "data": {
          "install": "Install and update the files in the configuration folder"
        }
      }
    },
    "abort": {
      "already_configured": "The OpenDTU Zero-Export EMS is already set up."
    }
  },
  "services": {
    "install_bundle": {
      "name": "Install bundle",
      "description": "Re-install the packaged EMS YAML file and the dashboard view into the configuration folder, overwriting them and keeping a `.bak` copy."
    }
  }
}
