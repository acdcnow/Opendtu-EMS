install_bundle:
  name: Install bundle
  description: >-
    Re-install the packaged EMS YAML file and the ready-made dashboard view into
    the configuration folder. Setup never overwrites an existing file — this
    action is the explicit way to update: the current file is kept as a `.bak`
    copy and replaced by the bundled one. Restart Home Assistant afterwards.
